spring:
  profiles:
    include: aws,s3

  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: false
        hbm2ddl:
          auto: none
logging.level:
  org.hibernate.SQL: debug